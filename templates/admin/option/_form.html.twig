{{ form_start(form) }}
{{ form_rest(form) }}
<!-- form_rest(form) pour afficher le reste du formulaire -->
<button class="btn btn-primary">{{ button|default('Enregistrer') }}</button>
{{ form_end(form) }}